{
  "name": "nighthound-storefront",
  "private": true,
  "version": "2.0.0",
  "description": "Static NightHound ecommerce storefront",
  "scripts": {
    "test": "playwright test qa.spec.js --reporter=line --workers=1",
    "validate:html": "html-validate *.html",
    "check": "npm run validate:html && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "html-validate": "^10.11.1"
  }
}
